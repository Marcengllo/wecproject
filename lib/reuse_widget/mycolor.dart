import 'package:flutter/material.dart';

const backgroundpage = Color.fromARGB(255, 21, 21, 21);
const textcolor = Colors.white;
const membiru = Colors.blueAccent;
const backgroundcard = Color.fromARGB(255, 40, 40, 40);
const iconkuning = Colors.yellow;
